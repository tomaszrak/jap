%h1 Listing words

%table.table.table-striped.table-hover
  %tr
    %th Translation
    %th Kana
    %th Romaji
    %th Kanji
    %th
    %th
    %th

  - @words.each do |word|
    %tr
      %td= word.translation
      %td= word.kana
      %td= word.romaji
      %td= word.kanji
      %td= link_to 'Show', word
      %td= link_to 'Edit', edit_word_path(word)
      %td= link_to 'Destroy', word, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Word', new_word_path, class: "btn btn-primary btn-lg"

